# 项目开发文档

## 引入git操作功能：
1. 项目名称后面加上分支名称，底部actions 右侧显示当前分支的最新一条提交记录，展示提交id和提交信息（大于30个省略展示样式，鼠标移入title展示全部内容）
2. 点击名称后面分支名称弹框下拉切换分支，并同步更新提交展示信息
3. actions 新增拉取，同步，迁出（同切换分支功能），合并（注意：git操作按钮的样式需要区别于项目的操作，如果有合并冲突需要自动打开编辑器定位当前项目）